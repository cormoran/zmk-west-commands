# Runtime dependencies for `west zmk-renode-test`.
#
# The Renode smoke test compiles the workspace's zmk-studio-messages protos
# with `protoc` and imports the generated Python modules, which need the
# protobuf runtime. `protoc` itself is a system tool (install protobuf-compiler
# / the protoc binary separately -- the CI action installs both).
protobuf
